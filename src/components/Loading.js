import React, { Component } from 'react';
class Loading extends Component {
    render() {
        return(
               <b> one second, minting your nft...</b>
        )
    }
}

export default Loading;